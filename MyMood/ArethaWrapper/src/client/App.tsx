import * as React from 'react';
import { useState, useEffect } from 'react';

const App = (props: AppProps) => {
	return (
		<main>
			<h1> My Mood wrapper online!</h1>
		</main>
	);
};

interface AppProps { }

export default App;
